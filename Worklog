04/08/26 @ 1:13 am

starting the process of the mv3 rewrite. just did a full empty commit on a new branch of the github.

git checkout -b mv3_port
git push -u origin mv3_port
git rm -rf .
git commit --allow-empty -m "..."

the first thing one should do when writing an extension is create the manifest.json

manifest.json created, as well as other base .js files.

tomorrows goals:
	1. figure out how to redirect using the original query, appending -ai to the search
	2. finish the popup logic if redirection is straight forward
	
time of sign-off: 1:53 am

04/08/26 @ 12:32 pm

thinking over the problem of the overview purger. while the -ai append works, I think a better solution could be completely removing it from the HTML of the page entirely. I notice that it lives under the <div id="0dp5de"> tag. maybe I can intercept all google searches and delete that field in the html?

div IDs aren't necessarily stable, and can change between Google's public builds

a better solution is semantic IDs, like this one data-hveid="CA4QBQ"


the above ID changes between searches/browsers. going to move forward with a maintained list of SELECTORS, at the moment the most useful one seems to be "data-mcpr"

going to target that selector and remove it from the DOM.